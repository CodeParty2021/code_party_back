import requests
import pprint

# このファイルはローカルしか動かない認証のAPIテストだよ

# フロントでtokenをとってきてToken Hereにペーストしてから実行してね
"""
headers = {
    "Authorization": "Bearer {}".format(
        "Token Here"
    )
}


r_get = requests.get("http://localhost:8000/users/auth", headers=headers)

pprint.pprint(r_get.json())
"""
